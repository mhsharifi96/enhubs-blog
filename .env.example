# Base API URL for the backend blog service
API_BASE_URL=http://127.0.0.1:8000/api/blog

# Optionally expose for client-side (only if needed)
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/blog

# Pagination size used when fetching posts
API_PAGE_SIZE=6

# ISR/SSR cache revalidation window (seconds)
API_REVALIDATE_SECONDS=60
